<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team10.smarthospital.mapper.NurseMapper">
  <insert id="insertUser">
    INSERT INTO Nurse
      (user_id, department, employee_id, location, work_number)
    VALUES
      (#{userId}, #{department}, #{employeeId}, #{location}, #{workNumber})
  </insert>
  <select id="getUserByUserId" resultType="com.team10.smarthospital.model.entity.Nurse">
    SELECT
      user_id, department, employee_id, location, work_number, create_time, update_time
    FROM Nurse
      WHERE user_id=#{userId}
  </select>
    <select id="getDoctorByDepartment" resultType="com.team10.smarthospital.model.entity.Nurse">
      SELECT
        user_id, department, employee_id, location, work_number, create_time, update_time
      FROM
        Nurse
      WHERE
        department=#{department}
    </select>
</mapper>
